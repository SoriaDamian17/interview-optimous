import { Modal as MMT } from '@material-ui/core';
import styled from 'styled-components';

const Modal = styled(MMT)`
display: flex;
align-items: center;
justify-content: center;
`;

export {
    Modal,
};
